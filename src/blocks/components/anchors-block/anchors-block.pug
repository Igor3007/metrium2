mixin anchors-block(links)
    nav.anchors-block
        each link in links
            a(
                href = `#${link.anchor}`
                class = {active: link.active})
                = link.title
